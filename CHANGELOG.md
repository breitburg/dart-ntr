## 0.1.0

- Initial release.
- TCP control channel with heartbeat and response parsing.
- UDP screen streaming with JPEG reassembly (top and bottom screens).
- `NtrSession` façade, `NtrConfig`, `NtrCommand` enhanced enum, sealed
  `NtrException` hierarchy.
- Built-in WriteMem patches (universal NFC, Pokémon Sun/Moon 1.1).
